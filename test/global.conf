config_file_path = { app_proto="test/app.proto"}

global_config = { thread_num = 4,
                  log_file_path="stdout",
}

session_config = { bucket_num = 16383,
                   ff_bucket_num = 4095,
                   session_expire_time = 30,
                   hash_name = "hash_xor",
                   session_result ="/tmp/session.csv",
}


